{
	"name": "kalium",
	"version": "0.0.0",
	"private": true,
	"workspaces": ["apps/*", "packages/*"],
	"scripts": {
		"build": "turbo run build",
		"dev": "turbo run dev --parallel"
	},
	"devDependencies": {
		"turbo": "^2.4.0"
	},
	"packageManager": "bun@1.1.42",
	"engines": {
		"node": ">=20.0.0",
		"bun": ">=1.0.0"
	}
}
